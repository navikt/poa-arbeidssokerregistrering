# E2E tester (Playwright)

Denne mappen inneholder en happy-path test for arbeidssokerregistreringen.

## Kjor lokalt

```bash
pnpm test:e2e:install
pnpm test:e2e
```

Testene starter appen med mock-flagg automatisk via `playwright.config.ts`.
